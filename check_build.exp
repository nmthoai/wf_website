#!/usr/bin/expect -f
set term xterm
set password "iL0vemyf@mily22717"
set ip "194.233.68.225"
spawn ssh -o StrictHostKeyChecking=no root@$ip
expect {
    "assword:" { send "$password\r"; exp_continue }
    "# " { send "cd /root/wf_website\r" }
}
expect "# " { send "docker compose up -d --build\r" }
expect "# " { send "exit\r" }
expect eof
